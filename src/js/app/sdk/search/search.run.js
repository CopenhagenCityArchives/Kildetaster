define([

], function() {

    var searchRun = ['$trace', function searchRun($trace) {

        // Enable this to get debug info about transitions in the console
        // https://ui-router.github.io/ng1/docs/1.0.0/enums/trace.category.html
        //$trace.enable('TRANSITION');

    }];

    return searchRun;
});